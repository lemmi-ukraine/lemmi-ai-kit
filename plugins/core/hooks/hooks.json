{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write|Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --no-project --python \">=3.11\" python -B \"${CLAUDE_PLUGIN_ROOT}/hooks/gate_learnings_drain.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
